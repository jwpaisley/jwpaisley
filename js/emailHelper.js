var config = require('./../config.json');
var lacewood = require('lacewood');
var mailer = require('express-mailer');

module.exports = {
    sendEmail: function(address, subject, contents){
      
    }
}
